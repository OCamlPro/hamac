;; This file is automatically generated by `marmiton` and MUST NOT be
;; edited manually.

;; Library [hamac-provisioning] rule
(library

  ;; The library name
  (name hamac_provisioning)

  ;; The library's public name
  (public_name hamac-provisioning)

  ;; Underlying modules are wrapped in module [Hamac-provisioning]
  (wrapped true)

  ;; Library's dependencies
  (libraries
    bos
    fmt
    fpath
    jingoo
    logs
    yaml build_info)

  (inline_tests)

  ;; Library's preprocessors
  (preprocess (pps
    ppx_deriving.std
    ppx_inline_test))
)
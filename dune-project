(lang dune 3.14)

;; This file is automatically generated by `marmiton` and MUST NOT be
;; edited manually.

;; Enabling cram tests
(cram enable)

;; Tells dune to generate opam files in `opam` directory
(generate_opam_files true)
(opam_file_location inside_opam_directory)

;; Using the dune_site plugin
(using dune_site 0.1)

;; Using menhir extension
(using menhir 3.0)

;; Project's name
(name hamac)


;; Listing packages' informations

(package
  (name hamac-provisioning)
  (allow_empty)
  (depends
    (bos :build)
    (fmt :build)
    (fpath :build)
    (jingoo :build)
    (logs :build)
    (opam-marmiton (and (>= 0.1.0) :with-dev-setup))
    (yaml :build)
    (ppx_deriving :build)
    (ppx_inline_test :build)
  )
)

(package
  (name hamac)
  (allow_empty)
  (depends
    (bos :build)
    (cmdliner :build)
    (cohttp-lwt-unix :build)
    (fmt :build)
    (fpath :build)
    (hamac-provisioning :build)
    (jingoo :build)
    (logs :build)
    (lwt :build)
    (opam-marmiton (and (>= 0.1.0) :with-dev-setup))
    (yaml :build)
    (yojson :build)
    (ppx_deriving :build)
  )
)

(package
  (name hamac-discovery)
  (allow_empty)
  (depends
    (cohttp-lwt-unix :build)
    (fmt :build)
    (hamac-provisioning :build)
    (logs :build)
    (lwt :build)
    (opam-marmiton (and (>= 0.1.0) :with-dev-setup))
    (yojson :build)
  )
)


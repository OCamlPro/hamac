name: CI

on:
  push:
    branches: [master]
  pull_request:

defaults:
  run:
    shell: bash

jobs:
  build:
    runs-on: docker
    env:
      OPAMROOTISOK: "1"
      OPAMYES: "1"
      OPAMCONFIRMLEVEL: unsafe-yes
      DEBIAN_FRONTEND: noninteractive
    steps:
      # Les durées sont relevées pour savoir où passe le temps du job avant
      # d'essayer de le réduire : une première tentative de cache opam a été
      # poussée sans cette mesure, n'a rien fait gagner et a cassé le job.
      - name: Install git + opam + OCaml + build deps
        run: |
          set -eux
          t=$(date +%s)
          apt-get update
          apt-get install -y --no-install-recommends \
            git ca-certificates opam ocaml m4 pkg-config build-essential libyaml-dev
          echo "TIMING apt=$(($(date +%s)-t))s"

      - name: Checkout (URL publique, authentifié)
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -eu   # pas de -x : ne pas logger le token
          # GITHUB_SERVER_URL pointe le nom interne http://forgejo:3000, non
          # résolvable depuis les conteneurs de job -> on passe par l'URL
          # publique (le conteneur a un accès Internet : il a fait apt).
          PUB="https://forge.ocamlpro.com"
          echo "clone ${PUB}/${GITHUB_REPOSITORY}.git ref=${GITHUB_REF_NAME}"
          git config --global --add safe.directory "$PWD"
          AUTH=$(printf 'x-access-token:%s' "$GH_TOKEN" | base64 -w0)
          git config --global http."${PUB}/".extraheader "Authorization: Basic ${AUTH}"
          git init -q .
          git remote add origin "${PUB}/${GITHUB_REPOSITORY}.git"
          git fetch --depth 1 origin "${GITHUB_REF_NAME}"
          git checkout -q -f FETCH_HEAD
          echo "checked out: $(git log --oneline -1)"

      - name: opam init + switch (system OCaml) + deps
        run: |
          set -eux
          t=$(date +%s)
          opam init --bare --disable-sandboxing -y
          t_init=$(($(date +%s)-t)); t=$(date +%s)
          opam switch create . ocaml-system --no-install -y
          t_switch=$(($(date +%s)-t)); t=$(date +%s)
          opam install . --deps-only --with-test -y
          t_deps=$(($(date +%s)-t))
          echo "TIMING opam_init=${t_init}s switch_create=${t_switch}s install_deps=${t_deps}s"
          # Taille des deux candidats au cache, pour juger du coût d'un
          # aller-retour par le serveur de cache.
          du -sh ~/.opam ~/.opam/download-cache _opam 2>/dev/null || true

      - name: Build
        run: |
          set -eux
          t=$(date +%s)
          opam exec -- dune build
          echo "TIMING build=$(($(date +%s)-t))s"

      - name: Test
        run: |
          set -eux
          t=$(date +%s)
          opam exec -- dune runtest
          echo "TIMING test=$(($(date +%s)-t))s"

      # Les tests cram ont leurs propres fixtures, hermétiques. Cette étape
      # vérifie les fichiers réellement livrés (providers, exemples, profils,
      # bundles) : ils doivent rester valides et rendables, sinon un
      # utilisateur qui suit le README tombe sur une erreur.
      - name: Smoke test des fichiers livrés
        run: |
          set -eux
          opam exec -- dune exec -- hamac validate \
            providers/*.sieste.yml \
            examples/enterprise-stack/*.sieste.yml \
            provisioning-profiles/*.yaml \
            templates/bundles/*/bundle.yaml
          opam exec -- dune exec -- hamac plan examples/enterprise-stack/*.sieste.yml
          opam exec -- dune exec -- hamac resolve examples/enterprise-stack/*.sieste.yml
          opam exec -- dune exec -- hamac provision-dryrun \
            --profile=provisioning-profiles/dev-workstation-demo.yaml

  # Construit l'image du serveur de découverte et vérifie qu'elle démarre.
  # Tourne sur le label `dind`, seul environnement où le daemon Docker est
  # disponible (cf. CLAUDE.md : pas de `container:` au niveau job).
  #
  # Sur master, l'image est publiée sur le registre de conteneurs de Forgejo
  # (`forge.ocamlpro.com/oss/hamac-discovery`), public comme le dépôt.
  image-discovery:
    runs-on: dind
    # Chaque step déclare `shell: sh` : l'image du label dind est une Alpine,
    # qui n'a pas bash, et le `defaults.run.shell: bash` global s'y applique
    # sinon — le job échouait dès son premier step, avant même de pouvoir
    # installer bash (exec: "bash": executable file not found in $PATH).
    # Un `defaults` au niveau job n'a pas suffi : le runner ne l'honore pas.
    steps:
      - name: Outils (l'image du label dind est une Alpine nue)
        shell: sh
        run: apk add --no-cache git

      # Le label `dind` fait tourner le job DANS l'image docker:dind, en
      # privileged, mais le runner est configuré avec `docker_host: "-"` : il
      # n'injecte aucun socket, et comme il exécute les steps sans passer par
      # l'entrypoint de l'image, le daemon n'est jamais démarré. D'où
      #   dial unix /var/run/docker.sock: connect: no such file or directory
      # On le démarre donc ici — c'est précisément ce que le privileged permet.
      - name: Démarre le daemon Docker
        shell: sh
        run: |
          set -eux
          dockerd --host=unix:///var/run/docker.sock >/tmp/dockerd.log 2>&1 &
          for _ in $(seq 1 60); do
            if docker info >/dev/null 2>&1; then break; fi
            sleep 1
          done
          docker info || { echo "--- dockerd.log ---"; cat /tmp/dockerd.log; exit 1; }

      - name: Checkout (URL publique, authentifié)
        env:
          GH_TOKEN: ${{ github.token }}
        shell: sh
        run: |
          set -eu
          PUB="https://forge.ocamlpro.com"
          git config --global --add safe.directory "$PWD"
          AUTH=$(printf 'x-access-token:%s' "$GH_TOKEN" | base64 -w0)
          git config --global http."${PUB}/".extraheader "Authorization: Basic ${AUTH}"
          git init -q .
          git remote add origin "${PUB}/${GITHUB_REPOSITORY}.git"
          git fetch --depth 1 origin "${GITHUB_REF_NAME}"
          git checkout -q -f FETCH_HEAD

      # Le binaire est lié dynamiquement à la glibc de la machine de build ;
      # on le compile donc dans debian-12 (glibc 2.36), la même base que
      # l'image runtime, sinon il ne démarrerait pas dedans.
      - name: Compile le binaire dans une image debian-12
        shell: sh
        run: |
          set -eux
          docker run --rm -v "$PWD":/src -w /src ocaml/opam:debian-12-ocaml-5.3 \
            bash -c 'sudo chown -R opam:opam /src \
                     && opam install -y --deps-only ./opam/hamac-provisioning.opam ./opam/hamac-discovery.opam \
                     && opam exec -- dune build --profile=release pkgs/hamac-discovery'

      - name: Assemble le contexte et construit l'image
        shell: sh
        run: |
          set -eux
          cp _build/default/pkgs/hamac-discovery/main.exe docker/hamac-discovery/discovery_server.exe
          rm -rf docker/hamac-discovery/bundles docker/hamac-discovery/profiles
          cp -r templates/bundles     docker/hamac-discovery/bundles
          cp -r provisioning-profiles docker/hamac-discovery/profiles
          # Les images OS dérivées (raw.zst + .bmap) ne sont pas générées ici :
          # cf. doc/PROVISIONING_SPEC.md §3.1. Le Dockerfile les COPY, donc le
          # répertoire doit exister même vide.
          mkdir -p docker/hamac-discovery/images
          docker build --build-arg "VCS_REF=${GITHUB_SHA}" \
            -t "hamac-discovery:${GITHUB_SHA}" docker/hamac-discovery/

      # Le curl est lancé DANS le conteneur plutôt que depuis le job : selon
      # que le daemon tourne ici ou à côté, un port publié n'est pas
      # forcément joignable sur localhost depuis le job. Passer par l'API du
      # daemon évite la question, et l'image embarque déjà curl pour son
      # propre HEALTHCHECK.
      - name: L'image démarre et répond sur /health
        shell: sh
        run: |
          set -eux
          docker run -d --name discovery "hamac-discovery:${GITHUB_SHA}"
          ok=0
          for _ in $(seq 1 30); do
            if docker exec discovery curl -fsS http://localhost:8877/health; then ok=1; break; fi
            sleep 1
          done
          docker logs discovery
          docker rm -f discovery
          [ "$ok" = 1 ]

      # Publiée seulement depuis master, et jamais depuis une pull request :
      # une PR ne doit pas pouvoir pousser une image sous le nom du projet.
      # Le registre de conteneurs est celui de Forgejo, donc public comme le
      # dépôt.
      #
      # L'authentification demande un secret dédié : le token éphémère du job
      # ne porte pas `write:package` (essayé, le push échoue sans même créer
      # le dépôt d'image — NAME_UNKNOWN au registre). Tant que REGISTRY_TOKEN
      # n'existe pas, l'étape le dit et passe son tour, plutôt que de faire
      # échouer un run par ailleurs sain pour une publication optionnelle.
      #
      # Pour l'activer : créer dans les paramètres du dépôt un secret
      # REGISTRY_TOKEN portant le droit `write:package`, et si besoin
      # REGISTRY_USER (défaut : le propriétaire du dépôt).
      - name: Publie l'image sur le registre Forgejo
        if: github.event_name == 'push' && github.ref_name == 'master'
        shell: sh
        env:
          REGISTRY: forge.ocamlpro.com
          REGISTRY_TOKEN: ${{ secrets.REGISTRY_TOKEN }}
          REGISTRY_USER: ${{ secrets.REGISTRY_USER }}
        run: |
          set -eu   # pas de -x : ne pas logger le token
          OWNER=$(echo "${GITHUB_REPOSITORY%%/*}" | tr '[:upper:]' '[:lower:]')
          IMAGE="${REGISTRY}/${OWNER}/hamac-discovery"
          if [ -z "${REGISTRY_TOKEN:-}" ]; then
            echo "REGISTRY_TOKEN absent : image construite et vérifiée, publication ignorée."
            echo "Pour publier ${IMAGE} : créer ce secret avec le droit write:package."
            exit 0
          fi
          echo "$REGISTRY_TOKEN" | docker login "$REGISTRY" \
            -u "${REGISTRY_USER:-$OWNER}" --password-stdin
          docker tag "hamac-discovery:${GITHUB_SHA}" "${IMAGE}:${GITHUB_SHA}"
          docker tag "hamac-discovery:${GITHUB_SHA}" "${IMAGE}:latest"
          docker push "${IMAGE}:${GITHUB_SHA}"
          docker push "${IMAGE}:latest"
          docker logout "$REGISTRY"
          echo "publié : ${IMAGE}:${GITHUB_SHA} et ${IMAGE}:latest"
